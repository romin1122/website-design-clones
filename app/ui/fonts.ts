import { Inter, Old_Standard_TT, EB_Garamond } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const serif = EB_Garamond({
  subsets: ['latin'],
  weight: ['400', '700'],
});
